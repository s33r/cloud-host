# template

Template Repository


Base Image:
    - /server = cloud-host express web server
    - /client = client application (empty)
    - /api    = express api app (empty)

API Image
    - /api    = node api app

Client Image
    - /client = browser site app
